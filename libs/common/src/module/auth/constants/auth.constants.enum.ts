export enum AUTH_STRATEGY {
  JWT = 'jwt',
  SESSION = 'session',
}

export enum RoleType {
  USER = 'USER',
  ADMIN = 'ADMIN',
}
export enum TokenType {
  ACCESS = 'ACCESS',
  REFRESH = 'REFRESH',
}
